name: 'Install Qt Dev Action'
description: 'Installs Qt modules using the online installer'
branding:
  color: 'green'
  icon: 'download-cloud'  
inputs:
  packages:
    description: 'The desired packages to install'
    required: true
#outputs:
  #time: # id of output
    #description: 'The time we greeted you'
runs:
  using: 'node12'
  main: 'src/install_qt.js'
